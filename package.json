{
  "name": "purrfect",
  "version": "0.0.11",
  "description": "Collection of composable tools and abstractions for building metadata extraction pipelines, aggregations, indexes, visualizations, and metadata endpoints.",
  "author": "Noodlestan Collective",
  "license": "MIT",
  "private": true,
  "packageManager": "npm@10.2.3",
  "workspaces": [
    "libs/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "turbo run build:clean",
    "build": "turbo run build",
    "lint": "prettier . -c && turbo run lint",
    "lint:fix": "prettier . -c --write && turbo run lint:fix",
    "test": "turbo run test",
    "ci": "turbo run ci",
    "dev": "turbo run dev --ui tui"
  },
  "devDependencies": {}
}
